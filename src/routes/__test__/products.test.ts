describe('products routes', () => {
  it('should return true for a truthy statement', () => {
    const expression = true;

    expect(expression).toBe(true);
  });

  it.todo('routes to /characters');
});
